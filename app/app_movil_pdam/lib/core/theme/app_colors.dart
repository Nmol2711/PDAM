import 'package:flutter/material.dart';

class AppColors {
  // ==============================
  // COLORES PRINCIPALES (PALETA PET APP)
  // ==============================

  // Verde principal (botones, acciones principales)
  static const Color primaryGreen = Color(0xFF34C759);

  // Turquesa secundario (iconos, elementos de apoyo)
  static const Color accentTurquoise = Color(0xFF30B0C7);

  // Naranja acento (recordatorios, alertas de comida)
  static const Color accentOrange = Color(0xFFFF8A3D);

  // ==============================
  // MODO OSCURO
  // ==============================

  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);

  // Se mantiene el nombre pero adaptado a la nueva paleta
  static const Color purpleBackgroud = Color(0xFF34C759);

  // Se mantiene el nombre pero adaptado a verde principal
  static const Color primaryPurple = Color(0xFF34C759);

  // ==============================
  // MODO CLARO
  // ==============================

  static const Color lightBackground = Color(0xFFF7F9F8);
  static const Color lightCard = Colors.white;

  // ==============================
  // TEXTO
  // ==============================

  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Colors.white;

  // ==============================
  // CARDS TEMÁTICAS (Dashboard)
  // ==============================

  // Card comida
  static const Color foodCard = Color(0xFFE8F8F1);

  // Card agua
  static const Color waterCard = Color(0xFFE6F7FB);

  // Card snacks
  static const Color snackCard = Color(0xFFFFF3E6);

  // ==============================
  // ESTADOS
  // ==============================

  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF8A3D);
  static const Color info = Color(0xFF30B0C7);
}