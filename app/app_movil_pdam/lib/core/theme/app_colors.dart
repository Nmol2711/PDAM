import 'package:flutter/material.dart';

class AppColors {
  // Colores base del diseño oscuro (tu imagen)
  static const Color darkBackground = Color(0xFF1E1E2C);
  static const Color darkCard = Color(0xFF27293D);
  static const Color purpleBackgroud = Color(0xFF866EFD);
  static const Color primaryPurple = Color(0xFF917CFF);
  static const Color accentTurquoise = Color(0xFF4DE1C1);

  // Colores para el modo claro (versiones suavizadas)
  static const Color lightBackground = Color(0xFFF8F9FE);
  static const Color lightCard = Colors.white;
}
